import { eq } from "drizzle-orm";
import { requireDb } from "../src/db";
import { organizations,publicationPolicies,roles,studyPrograms } from "../src/db/schema";
const ROLE_NAMES:Record<string,string>={ADMIN_SYSTEM:"Admin Sistem",ADMIN_DATA:"Admin Data",KAPRODI:"Kaprodi",GKM:"GKM",SEKJUR:"Sekjur",KAJUR:"Kajur",VIEWER_INTERNAL:"Viewer Internal"};
const POLICIES=[{subjectType:"STRATEGIC_STATEMENT",requiredLifecycleStatus:"EFFECTIVE",allowedFields:["statementType","statement","versionNumber","effectiveFrom"]},{subjectType:"KPI_MEASUREMENT",requiredLifecycleStatus:"APPROVED",allowedFields:["kpi","period","actualValue","achievementPercent","status","publicSummary"]},{subjectType:"EVALUATION",requiredLifecycleStatus:"APPROVED",allowedFields:["subject","period","publicSummary","recommendationSummary"]}];
async function main(){const db=requireDb();let[org]=await db.select().from(organizations).where(eq(organizations.code,"KEMARITIMAN")).limit(1);if(!org){const x=await db.insert(organizations).values({type:"UPPS",code:"KEMARITIMAN",name:"Jurusan Kemaritiman"});[org]=await db.select().from(organizations).where(eq(organizations.id,Number(x[0].insertId))).limit(1);}for(const[code,name]of Object.entries(ROLE_NAMES)){const[found]=await db.select().from(roles).where(eq(roles.code,code)).limit(1);if(!found)await db.insert(roles).values({code,name});}for(const program of[{code:"D3-NAUTIKA",name:"D3 Nautika"},{code:"D3-KPN",name:"D3 Ketatalaksanaan Pelayaran Niaga"}]){const[found]=await db.select().from(studyPrograms).where(eq(studyPrograms.code,program.code)).limit(1);if(!found)await db.insert(studyPrograms).values({organizationId:org.id,level:"D3",...program});}for(const p of POLICIES){const[found]=await db.select().from(publicationPolicies).where(eq(publicationPolicies.subjectType,p.subjectType)).limit(1);if(!found)await db.insert(publicationPolicies).values({subjectType:p.subjectType,policyVersion:1,allowedFields:p.allowedFields,requiredLifecycleStatus:p.requiredLifecycleStatus,requiresApproval:true,status:"ACTIVE"});}console.log("Foundation seed selesai.");}
main().catch(e=>{console.error(e);process.exit(1)});
