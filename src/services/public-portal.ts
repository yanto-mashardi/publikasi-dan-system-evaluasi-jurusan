import { and, eq } from "drizzle-orm";
import { db } from "@/src/db";
import { evaluations,followups,kpiMeasurements,kpis,kpiTargets,publications,recommendations,strategicStatements } from "@/src/db/schema";
export async function getPublicKpis(){if(!db)return[];return db.select({id:kpiMeasurements.id,code:kpis.code,name:kpis.name,period:kpiMeasurements.period,targetValue:kpiTargets.targetValue,actualValue:kpiMeasurements.actualValue,achievementPercent:kpiMeasurements.achievementPercent,status:kpiMeasurements.status,publicSummary:publications.publicSummary,publishedAt:publications.publishedAt}).from(publications).innerJoin(kpiMeasurements,and(eq(publications.subjectType,"KPI_MEASUREMENT"),eq(publications.subjectId,kpiMeasurements.id))).innerJoin(kpis,eq(kpiMeasurements.kpiId,kpis.id)).innerJoin(kpiTargets,and(eq(kpiTargets.kpiId,kpis.id),eq(kpiTargets.period,kpiMeasurements.period))).where(and(eq(publications.visibility,"PUBLIC"),eq(publications.status,"PUBLISHED")));}
export async function getPublicStatements(){if(!db)return[];return db.select({id:strategicStatements.id,type:strategicStatements.statementType,statement:strategicStatements.statement,version:strategicStatements.versionNumber,publishedAt:publications.publishedAt}).from(publications).innerJoin(strategicStatements,and(eq(publications.subjectType,"STRATEGIC_STATEMENT"),eq(publications.subjectId,strategicStatements.id))).where(and(eq(publications.visibility,"PUBLIC"),eq(publications.status,"PUBLISHED")));}
export async function getPublicEvaluations(){if(!db)return[];const rows=await db.select({id:evaluations.id,subjectType:evaluations.subjectType,subjectId:evaluations.subjectId,period:evaluations.period,publicSummary:evaluations.publicSummary,publishedAt:publications.publishedAt}).from(publications).innerJoin(evaluations,and(eq(publications.subjectType,"EVALUATION"),eq(publications.subjectId,evaluations.id))).where(and(eq(publications.visibility,"PUBLIC"),eq(publications.status,"PUBLISHED")));return Promise.all(rows.map(async row=>{const actions=await db.select({progress:followups.progressPercent,status:followups.status}).from(recommendations).innerJoin(followups,eq(recommendations.id,followups.recommendationId)).where(eq(recommendations.evaluationId,row.id));const progress=actions.length?Math.round(actions.reduce((a,x)=>a+Number(x.progress),0)/actions.length):null;return{...row,followupProgress:progress,followupCount:actions.length};}));}
