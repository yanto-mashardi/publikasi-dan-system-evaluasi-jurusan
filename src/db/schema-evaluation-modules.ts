import {bigint,date,decimal,json,mysqlTable,text,timestamp,uniqueIndex,varchar} from "drizzle-orm/mysql-core";
const id=(name="id")=>bigint(name,{mode:"number"}).autoincrement().primaryKey();
export const evaluationModules=mysqlTable("evaluation_modules",{id:id(),code:varchar("code",{length:80}).notNull(),name:varchar("name",{length:255}).notNull(),description:text("description"),scopeType:varchar("scope_type",{length:30}).notNull().default("PRODI"),status:varchar("status",{length:30}).notNull().default("ACTIVE"),createdAt:timestamp("created_at").defaultNow().notNull(),updatedAt:timestamp("updated_at").defaultNow().notNull()},t=>[uniqueIndex("evaluation_module_code_uq").on(t.code)]);
export const evaluationPeriods=mysqlTable("evaluation_periods",{id:id(),organizationId:bigint("organization_id",{mode:"number"}).notNull(),label:varchar("label",{length:100}).notNull(),academicYear:varchar("academic_year",{length:20}).notNull(),semester:varchar("semester",{length:20}).notNull(),status:varchar("status",{length:30}).notNull().default("OPEN"),createdBy:bigint("created_by",{mode:"number"}).notNull(),createdAt:timestamp("created_at").defaultNow().notNull(),updatedAt:timestamp("updated_at").defaultNow().notNull()},t=>[uniqueIndex("evaluation_period_org_label_uq").on(t.organizationId,t.label)]);
export const evaluationModuleSnapshots=mysqlTable("evaluation_module_snapshots",{id:id(),moduleId:bigint("module_id",{mode:"number"}).notNull(),organizationId:bigint("organization_id",{mode:"number"}).notNull(),studyProgramId:bigint("study_program_id",{mode:"number"}),period:varchar("period",{length:50}).notNull(),sourceScore:decimal("source_score",{precision:10,scale:4}),sourceCompleteness:decimal("source_completeness",{precision:7,scale:2}).notNull().default("0"),sourceCount:bigint("source_count",{mode:"number"}).notNull().default(0),snapshotData:json("snapshot_data").notNull(),generatedBy:bigint("generated_by",{mode:"number"}).notNull(),generatedAt:timestamp("generated_at").defaultNow().notNull()},t=>[uniqueIndex("evaluation_module_snapshot_uq").on(t.moduleId,t.organizationId,t.studyProgramId,t.period)]);
export const accreditationIndicatorModuleSources=mysqlTable("accreditation_indicator_module_sources",{id:id(),frameworkId:bigint("framework_id",{mode:"number"}).notNull(),indicatorId:bigint("indicator_id",{mode:"number"}).notNull(),moduleId:bigint("module_id",{mode:"number"}).notNull(),contributionWeight:decimal("contribution_weight",{precision:7,scale:4}).notNull().default("1"),assignedBy:bigint("assigned_by",{mode:"number"}).notNull(),status:varchar("status",{length:30}).notNull().default("ACTIVE"),createdAt:timestamp("created_at").defaultNow().notNull(),updatedAt:timestamp("updated_at").defaultNow().notNull()},t=>[uniqueIndex("indicator_module_source_uq").on(t.frameworkId,t.indicatorId,t.moduleId)]);

export const accreditationRtmSessions=mysqlTable("accreditation_rtm_sessions",{
 id:id(),organizationId:bigint("organization_id",{mode:"number"}).notNull(),period:varchar("period",{length:50}).notNull(),title:varchar("title",{length:255}).notNull(),meetingDate:date("meeting_date"),status:varchar("status",{length:30}).notNull().default("DRAFT"),summary:text("summary"),strengths:text("strengths"),gaps:text("gaps"),conclusion:text("conclusion"),signedDocumentKey:varchar("signed_document_key",{length:1000}),createdBy:bigint("created_by",{mode:"number"}).notNull(),approvedBy:bigint("approved_by",{mode:"number"}),createdAt:timestamp("created_at").defaultNow().notNull(),updatedAt:timestamp("updated_at").defaultNow().notNull()
},t=>[uniqueIndex("accreditation_rtm_org_period_uq").on(t.organizationId,t.period)]);

export const accreditationRtmItems=mysqlTable("accreditation_rtm_items",{
 id:id(),sessionId:bigint("session_id",{mode:"number"}).notNull(),assessmentId:bigint("assessment_id",{mode:"number"}).notNull(),discussionNote:text("discussion_note"),finding:text("finding"),rootCause:text("root_cause"),recommendation:text("recommendation"),actionPlan:text("action_plan"),ownerName:varchar("owner_name",{length:255}),dueDate:date("due_date"),followupStatus:varchar("followup_status",{length:30}).notNull().default("OPEN"),publicationDecision:varchar("publication_decision",{length:40}).notNull().default("INTERNAL_ONLY"),updatedBy:bigint("updated_by",{mode:"number"}).notNull(),createdAt:timestamp("created_at").defaultNow().notNull(),updatedAt:timestamp("updated_at").defaultNow().notNull()
},t=>[uniqueIndex("accreditation_rtm_item_uq").on(t.sessionId,t.assessmentId)]);
