"use client";
import { FormEvent,useEffect,useMemo,useState } from "react";
type Row=Record<string,any>;
export default function AccreditationConsole({permissions}:{permissions:string[]}){const has=(p:string)=>permissions.includes(p);const manage=has("accreditation.framework.manage"),assign=has("accreditation.assign");const[agencies,setAgencies]=useState<Row[]>([]),[frameworks,setFrameworks]=useState<Row[]>([]),[programs,setPrograms]=useState<Row[]>([]),[assignments,setAssignments]=useState<Row[]>([]),[frameworkId,setFrameworkId]=useState(0),[structure,setStructure]=useState<Row>({clusters:[],criteria:[],indicators:[],evidenceRequirements:[]}),[message,setMessage]=useState("");
async function request(url:string,method="GET",body?:unknown){const r=await fetch(url,{method,headers:body?{"content-type":"application/json"}:undefined,body:body?JSON.stringify(body):undefined});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(typeof d.error==="string"?d.error:"Operasi gagal");return d;}
async function load(){try{const[a,f,p,x]=await Promise.all([request("/api/internal/accreditation/agencies"),request("/api/internal/accreditation/frameworks"),request("/api/internal/study-programs"),request("/api/internal/accreditation/assignments")]);setAgencies(a);setFrameworks(f);setPrograms(p);setAssignments(x);}catch(e){setMessage(e instanceof Error?e.message:"Gagal memuat registry")}}
async function loadStructure(id:number){if(!id){setStructure({clusters:[],criteria:[],indicators:[],evidenceRequirements:[]});return}try{setStructure(await request(`/api/internal/accreditation/structure?frameworkId=${id}`));}catch(e){setMessage(e instanceof Error?e.message:"Gagal memuat struktur")}}
useEffect(()=>{void load()},[]);useEffect(()=>{void loadStructure(frameworkId)},[frameworkId]);const selected=useMemo(()=>frameworks.find(x=>x.id===frameworkId),[frameworks,frameworkId]);const fd=(e:FormEvent<HTMLFormElement>)=>new FormData(e.currentTarget);async function act(fn:()=>Promise<unknown>){try{await fn();setMessage("Perubahan tersimpan.");await load();await loadStructure(frameworkId);}catch(e){setMessage(e instanceof Error?e.message:"Operasi gagal")}}
return <div className="section">{message&&<div className="card" style={{marginBottom:16}}>{message}</div>}
<section className="grid"><div className="card"><h2>Lembaga Akreditasi</h2>{agencies.map(a=><p key={a.id}><b>{a.code}</b> — {a.name} <span className="badge">{a.status}</span></p>)}{manage&&<form className="form" onSubmit={e=>{e.preventDefault();const f=fd(e);void act(()=>request("/api/internal/accreditation/agencies","POST",{code:String(f.get("code")),name:String(f.get("name")),agencyType:String(f.get("type")||"LAM"),websiteUrl:String(f.get("website")||"")||undefined}))}}><h3>Tambah Lembaga</h3><input name="code" placeholder="LAM_INFOKOM" required/><input name="name" placeholder="Nama lembaga" required/><input name="type" defaultValue="LAM"/><input name="website" placeholder="https://..."/><button className="button">Tambah lembaga</button></form>}</div>
<div className="card"><h2>Framework / Instrumen</h2><select value={frameworkId} onChange={e=>setFrameworkId(Number(e.target.value))}><option value={0}>Pilih framework</option>{frameworks.filter(x=>x.lifecycleStatus!=="ARCHIVED").map(x=><option key={x.id} value={x.id}>{x.agencyCode} · {x.name} · v{x.versionNumber}</option>)}</select>{selected&&<p className="muted">{selected.instrumentType??"—"} · {selected.educationLevel??"semua jenjang"} · {selected.lifecycleStatus}</p>}{manage&&<form className="form" onSubmit={e=>{e.preventDefault();const f=fd(e);void act(()=>request("/api/internal/accreditation/frameworks","POST",{agencyId:Number(f.get("agencyId")),code:String(f.get("code")),name:String(f.get("name")),instrumentYear:Number(f.get("year"))||undefined,instrumentType:String(f.get("instrumentType")||"")||undefined,educationLevel:String(f.get("educationLevel")||"")||undefined,modality:String(f.get("modality")||"TATAP_MUKA"),regulationReference:String(f.get("regulation")||"")||undefined,sourceUrl:String(f.get("sourceUrl")||"")||undefined,versionNumber:Number(f.get("version"))||1}))}}><h3>Tambah Framework</h3><select name="agencyId" required><option value="">Pilih lembaga</option>{agencies.filter(x=>x.status==="ACTIVE").map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><input name="code" placeholder="LAM-X-2026-D3" required/><input name="name" placeholder="Nama instrumen" required/><input name="year" type="number" placeholder="Tahun"/><input name="instrumentType" placeholder="REGULER / PERPANJANGAN / ..."/><input name="educationLevel" placeholder="D3 / S1 / ..."/><input name="modality" defaultValue="TATAP_MUKA"/><input name="version" type="number" defaultValue="1"/><input name="regulation" placeholder="Referensi regulasi"/><input name="sourceUrl" placeholder="URL sumber resmi"/><button className="button">Tambah framework</button></form>}</div></section>
{frameworkId>0&&<><section className="grid"><div className="card"><h2>Klaster</h2>{(structure.clusters??[]).sort((a:Row,b:Row)=>a.sequence-b.sequence).map((x:Row)=><p key={x.id}><b>{x.code}</b> — {x.name} <span className="badge">{x.semanticGroup??"CUSTOM"}</span></p>)}{manage&&selected?.lifecycleStatus==="DRAFT"&&<form className="form" onSubmit={e=>{e.preventDefault();const f=fd(e);void act(()=>request("/api/internal/accreditation/structure","POST",{entity:"CLUSTER",frameworkId,code:String(f.get("code")),name:String(f.get("name")),semanticGroup:String(f.get("semanticGroup")||"")||undefined,sequence:Number(f.get("sequence"))||1}))}}><input name="code" placeholder="INPUT" required/><input name="name" placeholder="Nama klaster" required/><input name="semanticGroup" placeholder="INPUT / PROCESS / OUTPUT_OUTCOME"/><input name="sequence" type="number" defaultValue="1"/><button className="button">Tambah klaster</button></form>}</div>
<div className="card"><h2>Kriteria</h2>{(structure.criteria??[]).sort((a:Row,b:Row)=>a.sequence-b.sequence).map((x:Row)=><p key={x.id}><b>{x.code}</b> — {x.name}</p>)}{manage&&selected?.lifecycleStatus==="DRAFT"&&<form className="form" onSubmit={e=>{e.preventDefault();const f=fd(e);void act(()=>request("/api/internal/accreditation/structure","POST",{entity:"CRITERION",frameworkId,code:String(f.get("code")),name:String(f.get("name")),sequence:Number(f.get("sequence"))||1}))}}><input name="code" placeholder="K1" required/><input name="name" placeholder="Nama kriteria" required/><input name="sequence" type="number" defaultValue="1"/><button className="button">Tambah kriteria</button></form>}</div></section>
<section className="card"><h2>Indikator</h2>{(structure.indicators??[]).map((x:Row)=><p key={x.id}><b>{x.code}</b> — {x.name}</p>)}{manage&&selected?.lifecycleStatus==="DRAFT"&&<form className="form" onSubmit={e=>{e.preventDefault();const f=fd(e);const clusterId=Number(f.get("clusterId"));void act(()=>request("/api/internal/accreditation/structure","POST",{entity:"INDICATOR",frameworkId,criterionId:Number(f.get("criterionId")),code:String(f.get("code")),name:String(f.get("name")),description:String(f.get("description")||"")||undefined,unit:String(f.get("unit")||"")||undefined,weight:Number(f.get("weight"))||undefined,sequence:Number(f.get("sequence"))||1,clusterIds:clusterId?[clusterId]:[],primaryClusterId:clusterId||undefined}))}}><select name="criterionId" required><option value="">Pilih kriteria</option>{(structure.criteria??[]).filter((x:Row)=>x.status==="ACTIVE").map((x:Row)=><option key={x.id} value={x.id}>{x.code} — {x.name}</option>)}</select><select name="clusterId"><option value="">Klaster utama (opsional)</option>{(structure.clusters??[]).filter((x:Row)=>x.status==="ACTIVE").map((x:Row)=><option key={x.id} value={x.id}>{x.code} — {x.name}</option>)}</select><input name="code" placeholder="K1-I1" required/><input name="name" placeholder="Nama indikator" required/><textarea name="description" placeholder="Deskripsi indikator"/><input name="unit" placeholder="Unit (opsional)"/><input name="weight" type="number" step="0.0001" placeholder="Bobot (opsional)"/><input name="sequence" type="number" defaultValue="1"/><button className="button">Tambah indikator</button></form>}</section>
<section className="card"><h2>Kebutuhan Evidence</h2>{(structure.evidenceRequirements??[]).map((x:Row)=><p key={x.id}><b>{x.code}</b> — {x.description}</p>)}{manage&&selected?.lifecycleStatus==="DRAFT"&&(structure.indicators??[]).length>0&&<form className="form" onSubmit={e=>{e.preventDefault();const f=fd(e);void act(()=>request("/api/internal/accreditation/structure","POST",{entity:"EVIDENCE_REQUIREMENT",indicatorId:Number(f.get("indicatorId")),code:String(f.get("code")),description:String(f.get("description")),required:true,acceptableSubjectTypes:String(f.get("subjectTypes")||"").split(",").map(x=>x.trim()).filter(Boolean)}))}}><select name="indicatorId" required><option value="">Pilih indikator</option>{(structure.indicators??[]).filter((x:Row)=>x.status==="ACTIVE").map((x:Row)=><option key={x.id} value={x.id}>{x.code} — {x.name}</option>)}</select><input name="code" placeholder="EV-01" required/><textarea name="description" placeholder="Dokumen/data yang harus tersedia" required/><input name="subjectTypes" placeholder="KPI_MEASUREMENT,CURRICULUM,..."/><button className="button">Tambah kebutuhan evidence</button></form>}</section></>}
<section className="card"><h2>Assignment Framework ke Program Studi</h2>{assignments.map(x=><p key={x.id}><b>{x.studyProgramName}</b> → {x.agencyCode} / {x.frameworkName} <span className="badge">{x.assignmentStatus}{x.isPrimary?" · PRIMARY":""}</span></p>)}{assign&&<form className="form" onSubmit={e=>{e.preventDefault();const f=fd(e);void act(()=>request("/api/internal/accreditation/assignments","POST",{studyProgramId:Number(f.get("studyProgramId")),frameworkId:Number(f.get("frameworkId")),isPrimary:true,officialCoverageReference:String(f.get("coverage")||"")||undefined,notes:String(f.get("notes")||"")||undefined}))}}><select name="studyProgramId" required><option value="">Pilih Program Studi</option>{programs.filter(x=>x.status==="ACTIVE").map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><select name="frameworkId" required><option value="">Pilih framework</option>{frameworks.filter(x=>x.lifecycleStatus==="ACTIVE").map(x=><option key={x.id} value={x.id}>{x.agencyCode} · {x.name}</option>)}</select><input name="coverage" placeholder="Referensi cakupan resmi / SK / URL"/><input name="notes" placeholder="Catatan assignment"/><button className="button">Tautkan sebagai framework utama</button></form>}<p className="muted">LAM Teknik reference seed tidak ditautkan otomatis. Assignment dilakukan setelah cakupan resmi Prodi diverifikasi.</p></section></div>}
