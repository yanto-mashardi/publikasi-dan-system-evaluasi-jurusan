import { NextResponse } from "next/server";import { z } from "zod";import { requireDb } from "@/src/db";import { strategicStatements } from "@/src/db/schema";import { governanceScopes } from "@/src/db/schema-phase5";import { getSession } from "@/src/lib/auth";import { can,scopeAllows } from "@/src/lib/rbac";import { audit } from "@/src/lib/audit";import { attachGovernanceScope,validateGovernanceScope } from "@/src/services/governance-scope";
const input=z.object({organizationId:z.number().int().positive(),studyProgramId:z.number().int().positive().nullable().optional(),planId:z.number().int().positive().optional(),statementType:z.enum(["VISION","MISSION","OBJECTIVE","STRATEGY","SCIENTIFIC_VISION"]),code:z.string().optional(),sequence:z.number().int().positive().default(1),statement:z.string().min(5)});
export async function GET(){const s=await getSession();if(!s)return NextResponse.json({error:"Unauthorized"},{status:401});const db=requireDb();const rows=await db.select().from(strategicStatements);const scopes=await db.select().from(governanceScopes);const map=new Map(scopes.filter(x=>x.subjectType==="STRATEGIC_STATEMENT").map(x=>[x.subjectId,x]));return NextResponse.json(rows.map(x=>({...x,studyProgramId:map.get(x.id)?.studyProgramId??null})));}
export async function POST(req:Request){const s=await getSession();if(!s)return NextResponse.json({error:"Unauthorized"},{status:401});if(!(can(s,"data.create")||can(s,"program.update")))return NextResponse.json({error:"Forbidden"},{status:403});const p=input.safeParse(await req.json());if(!p.success)return NextResponse.json({error:p.error.flatten()},{status:400});if(p.data.statementType==="SCIENTIFIC_VISION"&&!p.data.studyProgramId)return NextResponse.json({error:"Scientific vision harus memiliki scope Program Studi."},{status:400});if(!(await validateGovernanceScope(p.data.organizationId,p.data.studyProgramId)))return NextResponse.json({error:"Program Studi tidak berada pada organisasi yang dipilih."},{status:409});if(!scopeAllows(s,p.data.organizationId,p.data.studyProgramId))return NextResponse.json({error:"Scope organisasi/Program Studi tidak sesuai."},{status:403});const db=requireDb();const {studyProgramId,...statement}=p.data;const x=await db.insert(strategicStatements).values({...statement,versionNumber:1,lifecycleStatus:"DRAFT"});const id=Number(x[0].insertId);await attachGovernanceScope({subjectType:"STRATEGIC_STATEMENT",subjectId:id,organizationId:p.data.organizationId,studyProgramId});await audit({actorId:s.userId,action:"CREATE",subjectType:"STRATEGIC_STATEMENT",subjectId:id,after:p.data});return NextResponse.json({id,status:"DRAFT",studyProgramId:studyProgramId??null},{status:201});}
