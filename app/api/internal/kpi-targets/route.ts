import { NextResponse } from "next/server";import { z } from "zod";import { requireDb } from "@/src/db";import { kpiTargets } from "@/src/db/schema";import { getSession } from "@/src/lib/auth";import { can } from "@/src/lib/rbac";import { audit } from "@/src/lib/audit";
const input=z.object({kpiId:z.number().int().positive(),period:z.string().min(4),targetValue:z.number()});
export async function POST(req:Request){const s=await getSession();if(!s)return NextResponse.json({error:"Unauthorized"},{status:401});if(!(can(s,"data.update")||can(s,"program.update")))return NextResponse.json({error:"Forbidden"},{status:403});const p=input.safeParse(await req.json());if(!p.success)return NextResponse.json({error:p.error.flatten()},{status:400});const db=requireDb();const x=await db.insert(kpiTargets).values({kpiId:p.data.kpiId,period:p.data.period,targetValue:String(p.data.targetValue)});const id=Number(x[0].insertId);await audit({actorId:s.userId,action:"SET_TARGET",subjectType:"KPI_TARGET",subjectId:id,after:p.data});return NextResponse.json({id},{status:201});}
