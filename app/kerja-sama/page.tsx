import Link from "next/link";
import { getPublishedCooperations } from "@/src/services/public-resources";
export const dynamic="force-dynamic";
export default async function CooperationPage(){const rows=await getPublishedCooperations();return <main className="shell"><section className="hero"><div className="eyebrow">Kemitraan</div><h1>Kerja Sama</h1><p className="muted">Informasi yang tampil adalah kerja sama yang sudah memperoleh approval dan publication record.</p></section><section className="section"><div className="grid">{rows.length?rows.map(x=><article className="card" key={x.id}><div className="eyebrow">{x.programName??"UPPS"} · {x.partnerType??"Mitra"}</div><h2>{x.partnerName}</h2><p>{x.scope}</p><p className="muted">{x.startDate??"—"} s.d. {x.endDate??"—"}</p><span className="badge">{x.cooperationStatus}</span><p>{x.implementationSummary??"Ringkasan implementasi belum tersedia."}</p></article>):<div className="card">Belum ada kerja sama yang dipublikasikan.</div>}</div></section><p><Link href="/">Kembali ke beranda</Link></p></main>}
